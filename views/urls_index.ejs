<% include partials/_header %>
    <div class="rounded" style="background-color:#b2bec3; padding-left: 20px; padding-right: 20px; height: 100%; width: 60%; margin-left: 20%; margin-right: 20%;">
      <!-- Add your EJS code here -->
      <% if(currentUser !== null || currentUser !== undefined) { %>
      <% for(key in urls) { %>
        <div style="display: flex; justify-content: space-between; align-items: center; width: auto; padding-bottom: 15px; padding-top: 15px; font-family: 'Courier New', Courier, monospace;">
          <a>  
            <%= key %> --->
          </a>
          <div>
            <a class="btn btn-link" style="position: relative; width:fit-content"  href="<%= urls[key]['longURL'] %>">
              <%= urls[key]["longURL"] %>
            </a>
            </div>
          <div>
            <form style="position: inherit" method="POST" action="/urls/<%= key %>/delete">
              <button class="btn btn-danger" style="vertical-align: middle;">  Delete 🗑️  </button>
            </form>
            <form action="/urls/<%= key %>">
                <button class="btn btn-primary" style="vertical-align: middle;" >  View/Edit  </button>
            </form>
          </div>
        </div>
      <% } %>
      <% } %>
    </div>
<% include partials/_footer %>
