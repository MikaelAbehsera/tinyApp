<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body>
      <!-- Add your EJS code here -->
      <% for(url in urls) { %>
          <p><%= urls[url] %></p>
      <% } %>
  </body>
</html>